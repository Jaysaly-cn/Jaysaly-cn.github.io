const {test}=require('node:test'),assert=require('node:assert/strict');
test('unsupported, empty and oversized inputs are rejected',async()=>{const {validateFile}=await import('../snaptext-core.mjs');for(const f of [{type:'application/pdf',size:1},{type:'image/png',size:0},{type:'image/jpeg',size:11*1024*1024}])assert.throws(()=>validateFile(f));validateFile({type:'image/png',size:100});});
test('pixel limit precedes resize; aspect ratio retained',async()=>{const {dimensions}=await import('../snaptext-core.mjs');assert.throws(()=>dimensions(5000,5000));assert.throws(()=>dimensions(0,30));assert.deepEqual(dimensions(4000,2000),{width:2500,height:1250,scaled:true});assert.deepEqual(dimensions(400,200),{width:400,height:200,scaled:false});});
test('raw recognition record does not depend on editor and exports a safe filename',async()=>{const {recognitionRecord,txtName}=await import('../snaptext-core.mjs');const r=recognitionRecord({name:'通知.png',language:'chi_sim',text:'出发 15:00',confidence:85,elapsed:1234.4,width:200,height:100});assert.equal(r.rawText,'出发 15:00');assert.equal(r.elapsedMs,1234);assert.equal(txtName('a/b:?.png'),'a_b__.txt');assert.equal(txtName('通知.png'),'通知.txt');});
