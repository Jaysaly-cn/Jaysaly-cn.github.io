# SnapText 开发验收 · 2026-09-24

本机 Chromium 内嵌浏览器，`http://127.0.0.1:8811/everyday/snaptext.html`。

1. 页面绘制 1200×400 中英合成图片，经真实 WASM LSTM 识别，耗时约 0.9 秒（含本机资源准备），内部置信度 89。英文 `Weekend plan: bring water. Leave at 15:00.` 与编号 `A2026-0924` 正常；中文出现词间多余空格。没有用预设文字填结果。
2. 经浏览器文件选择器上传 `artifacts/english.png`，选仅英文，真实识别得到 `Reading list / Bring water at 15:00. / Ticket A2026-0924`，约 0.9 秒、内部置信度 94。浏览器 error/warn 日志为空。
3. 将结果编辑为 `人工修订：Bring water at 16:00.` 后点击复制，剪贴板内容与编辑值一致。
4. 三份自建 PNG 经 Node 端同版本引擎实测：英文、中文在移除空白后 CER 为 0；空白图输出为空。仅为清晰合成开发样例，不能据此宣称日常照片准确率或首网加载性能。

尚未完成：手机实机性能、模糊/倾斜/多栏困难样例、识别取消与超时浏览器验收、公开部署与冷启动下载验证。Node 三例连续运行期间出现一条乱码语言加载 stderr 警告，尽管结果正常；需要复现与定位后再宣布稳定。详见 `ocr-evaluation.json` 原始输出。
