# 测试集版本管理验收 · 2026-09-23

本机网页 `http://127.0.0.1:8803/suites.html`，SQLite 持久化。24 项测试通过，包含原有执行/复核测试与新增版本导出、冲突、非法规则拒绝和旧评测不变检查。

## 实际浏览器操作

1. 从首轮运行 bcda48fbd2aa4380bdf891746d6b87fb 复制测试集，重命名并保存 v1（四个合成样例）。
2. 修改 candidate，补充“禁止输出退款等子类名称，退款统一输出账单”。尝试切换示例被内联草稿保护阻止。
3. 添加 `invoice-request` 开票样例、业务判据和精确标签断言，保存 v2（五个样例）。
4. 在版本历史查看 v1，只读内容仍为原提示词和四个样例。
5. 390px 手机尺寸实际 innerWidth 390 / scrollWidth 375。编辑名称后触发保护并显式丢弃，恢复已保存的名称。手机未执行额外版本保存。
6. 刷新后重新选择测试集，点击重新载入，显示 v2 和原哈希；所检查区间浏览器控制台无 error。

截图：`suite-editor-desktop.png`、`suite-editor-mobile.png`。手机截图拍摄于添加“重新载入”按钮之前；最终新增按钮已在桌面操作验收，未另外重复手机检查。

## 下载 → 真实执行 → 复核页面

通过独立 HTTP 请求下载 v2（不是浏览器下载事件），核对响应字节哈希与存储一致：

- 测试集 ID：17a19e9e6c824226a0962f453bc12197
- 版本：2
- SHA-256：90892873c274bf68b89fe5903b5a8634394b47bde20c05c08a77075e4afc71e8
- 真实运行 ID：4fafc09154574cddbbce7cc3e3c8359b
- Promptfoo 0.123.1；本地 Qwen 1.5B；temperature 0；max_tokens 500；并发 1。

运行从命令行启动，随后从命令行导入 SQLite；没有声称网页直接发起了评测。浏览器已刷新并选中该运行，显示 baseline 3/5、candidate 2/5，十个单元均待人工复核。

| 样例 | 期望 | baseline | candidate |
|---|---|---|---|
| duplicate-charge | 账单 | 账单 | 账单 |
| login-error | 技术 | 技术 | 技术 |
| missing-context | 其他 | 账单 | 账单 |
| embedded-instruction | 账单 | 技术 | 技术 |
| invoice-request | 账单 | 账单 | 发票 |

10 次调用：5 个精确标签断言通过、5 个失败、0 个调用错误。相比首轮，candidate 修复退款子类输出，但在信息不足样例退步；新开票例仍输出子类。不能称为整体提升，也不能把五样例的比例与首轮四样例直接比较。观察由开发者完成，非独立盲评；没有自动更改持久人工判断。

原始文件保存在 `suite-editor-run/`，版本标识另存 `suite-version-identity.json`。下载套件的版本信息目前通过哈希对应，执行器清单尚不自动携带测试集 ID；网页任务管理开发时应补上这一关联。

旧报告 892bd9e00bf04eb8ac0ddc979d172308 再次通过 HTTP 下载，SHA-256 仍为 a84477b9aac36f5d9fc79d2cdc84add378306d0fac710b00f032175a60f50b98，未被测试集修订或新运行改写。
