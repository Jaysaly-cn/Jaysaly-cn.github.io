# 慢上传与会话过期回归 · 2026-09-23

在请求体分块传输的中途推进会话时钟到1801秒，另一请求触发清理。修复前数据库被提前删除，活动上传失去工作空间。本问题在本项目和其他7个同源演示中分别复现；测试时钟是主动故障模拟，不声称发生于真实访客。

把active计数与上下文设置前移到接收请求体之前，读body、写入额度和模型额度检查纳入原有try/finally。正常返回、40KB超限413和写入额度429均释放计数；后续过期请求才删除空间。业务API、配额数值、模型、审核和导出逻辑不变。EvalDesk仍保留运行中/待核查后台任务的额外保护。

新增 tests/test_demo_upload.py 的3项回归：接受慢上传时数据库和schema保留；超大请求仍释放引用并允许回收；额度拒绝不泄漏活动计数。测试探针只在测试app注册，生产没有upload-probe接口。初始测试夹具被部分应用的根静态挂载遮挡，另有SQLite上下文不自动关闭导致Windows文件占用；修正路由顺序并显式关闭连接后再跑全套。

本项目完整 **49 项测试通过**，此前为46项。八项目合计307项通过，新增24项。可复现：`python -m pytest -q tests/test_demo_upload.py`，应在项目目录运行。Windows建议使用项目内 `--basetemp=data/pytest-<唯一名字>`。

演示进程已逐一更新，保留原隧道地址；更新会使旧Cookie会话失效，重新进入首页获得合成空间。公网验收检查首页与新会话资源列表均200，不能替代全部业务工作流或30分钟自然等待的验收。没有新增模型调用或改变生成质量声明。
