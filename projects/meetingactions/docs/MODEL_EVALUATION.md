# 免费本机模型实测

2026-09-23，Qwen2.5-1.5B-Instruct Q4_K_M，llama.cpp b11118、CPU4线程、8192上下文。所有材料合成，由开发助手运行并检查；不是独立人工标注或商业准确率。

保留三份记录：`live-model-baseline.json` 初版；`live-model-prompt.json` 加强引用要求后；`live-model.json` 支持严格发言人前缀定位后。同四条开发问题反复调试，不能当作独立评测集。

- 明确承诺：最终真实返回两项行动，负责人由紧邻发言人前缀验证，明天/下周一分别换算为 2026-09-24 / 2026-09-28，均为 draft。
- 否定：仍将“等待下次会议再决定公告”当作候选，这是误提取，需要退回；不能自动执行。
- 未知负责人：提取两项候选，owner 留空，日期词不明确则不换算；审核时需人工确定是否成立并补齐负责人。
- 注入：该例未通过校验，整批无行动写入。一次结构拒绝不代表普遍的注入防护；原始模型输出仍可能包含无效任务。

定位修正没有取消校验：只接受原文中确实紧邻该句的“姓名：”前缀，错误人名和重复片段仍被拒绝；测试覆盖该区别。整体产品依赖人工判断承诺是否成立，未提供自动追催或外部任务发布。
