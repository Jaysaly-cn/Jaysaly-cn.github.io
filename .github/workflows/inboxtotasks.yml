name: InboxToTasks verification
on:
  push:
    paths: ['projects/inboxtotasks/**', '.github/workflows/inboxtotasks.yml']
  pull_request:
    paths: ['projects/inboxtotasks/**', '.github/workflows/inboxtotasks.yml']
permissions:
  contents: read
jobs:
  verify:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: projects/inboxtotasks
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - run: pip install -r requirements-lock.txt
      - run: python -m pytest -q
      - run: node --check web/app.js
