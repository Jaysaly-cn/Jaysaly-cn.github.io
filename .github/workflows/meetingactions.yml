name: MeetingActions verification
on:
  push:
    paths: ['projects/meetingactions/**', '.github/workflows/meetingactions.yml']
  pull_request:
    paths: ['projects/meetingactions/**', '.github/workflows/meetingactions.yml']
permissions:
  contents: read
jobs:
  verify:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: projects/meetingactions
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - run: pip install -r requirements-lock.txt
      - run: python -m pytest -q
      - run: python -m evals.run
      - run: node --check web/app.js
