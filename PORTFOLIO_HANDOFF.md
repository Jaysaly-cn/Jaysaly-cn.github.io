# AI 作品集第一辑 · 收尾交接

2026-09-23。按用户要求停止扩展新项目，并关闭每两小时持续开发任务。

## 访问入口

- [陈列馆](https://jaysaly-cn.github.io/ai/)：11 款作品，2 个重点案例、9 个场景工具；首页仅保留一个展馆入口。
- 每件展品的“网页演示”在同一个 GitHub Pages 域名下，无需本地服务在线。支持合成资料操作、引用核对、修订、确认及 JSON 冻结交付。
- `projects/` 保留 11 款真实后端项目的源码、锁定依赖、测试、真实模型开发评测和运行说明。它们是本仓库内的源码镜像，不能称为 11 个独立 GitHub 仓库。

## 访问故障处理

用户报告 EvidenceBrief 临时入口显示 Cloudflare 1033；本机复核未发现运行中的 cloudflared 进程。临时隧道依赖开发机和连接进程，不能承担收尾后的长期入口。

已移除展馆与案例中的临时隧道链接，改为 11 个稳定路径的静态演示。不宣称完整后端已经云托管。历史文档里的公网验收是当时记录，不是当前可用性承诺。

## 网页演示范围

- EvidenceBrief、MeetingActions、ContentBench、ApplyTrack、InboxToTasks、StudyDeck、FeedbackLens 展示预置人工候选的引用核对、修订、确认和冻结流程；ContentBench 校验必带说明，ApplyTrack 要求补充经历，自测卡可展开答案。
- SupportOps 另提供关键词检索和转人工候选；不把字符串检索标为 BM25 或模型问答。
- DataBrief 在浏览器按渠道汇总两列 CSV，明确拒绝空值、非法整数和不支持的格式。
- ChangeLens 按行位置比较两个版本，保留待解释的影响；不识别移动或声称语义理解。
- EvalDesk 对输入标签逐行精确匹配；不调用模型，结果不是模型准确率。
- 全部使用 localStorage，记录仅在当前浏览器；可清除或恢复示例。确认前不可导出交付，后续修改不会重写旧快照。

这些网页演示是完整软件的展示补充，没有替换原有 Python 后端、SQLite 数据库或真实模型接入。真实模型服务需按各项目 README 启动；目前没有持续运行的公网模型服务。

## 图片与维护

11 张项目主题配图用内置 image_gen 生成，保存于 `assets/ai-covers/`。完整提示词保存在同目录 `prompts.json`。这些是概念插画，不是运行截图；产品真实截图和评测仍在各案例与源码中。

本次没有修改后端或继续 ApplyTrack 的跟进面板新功能。后续如重启开发，可先评估是否需要托管少量重点项目，再决定持续托管预算，不继续依赖临时域名。

## 验证

`node --test demos/ai/demo.test.cjs` 验证审核门槛、快照内容隔离、冻结不变、合成标记、存储不可用、快照上限与 11 份样例完整性。另有浏览器人工验收及全站本地引用检查。此次未改动后端，不重复报告为新增后端评测。
